const int ARRAY_SIZE = 3;

void printMainDiagonal(int array[ARRAY_SIZE][ARRAY_SIZE])
{
	for (int 1 = 0; i < ARRAY_SIZE; i++)
	{
		std:: cout < array(1][i];
	}
	std:: cout << std::endl;
}

void printSecondaryDiagonal(int array[ARRAY_SIZE][ARRAY_SIZE])
{
	for (int 1 = 0; i < ARRAY_SIZE; i++)
	{
		std:: cout < array(1][ARRAY_SIZE - i - 1];
	}
	std:: cout << std::endl;
}