#include <iostream>

using namespace std;

int main()
{
    int Area1 = 5 * 4;
    int Obikolka1 = 2 * (5 + 4);

    int Area2 = 7 * 9;
    int Obikolka2 = 2 * (7 + 9);

    cout << "The Area of the first rectangle is " << Area1 << " square metric units and its perimeter is " << Obikolka1 << " metric units." << endl;

    cout << "The Area of the second rectangle is " << Area2 << " square metric units and its perimeter is " << Obikolka2 << " metric units." << endl;
}
